name = "debrid-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Workers configuration
workers_dev = true

# Route configuration (optional - if you want to use a custom domain)
# routes = [
#   { pattern = "debrid.your-domain.com/*", zone_name = "your-domain.com" }
# ]

# Environment variables (optional)
# [env.production]
# name = "debrid-proxy-prod"
